import { type AlokaiContainer } from "@vue-storefront/middleware";
import { ConfigState } from "../../types/setup";
export declare const handleRetry: ({ alokai }: {
    alokai: AlokaiContainer;
}) => (retryCount: any, operation: any, error: any) => boolean;
export declare const linkFactory: ({ state, alokai }: {
    state: ConfigState;
    alokai: AlokaiContainer;
}) => import("@apollo/client/core").ApolloLink;
//# sourceMappingURL=linkHandlers.d.ts.map